[package]
name = "r-iva9/aery"
description = "More than a maid-like garbage collector for Luau and the Allure Ecosystem."
license = "MIT"
authors = ["r-iva9"]
version = "1.0.0"

registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

exclude = ["**"]
include = ["src", "src/*", "default.project.json", "wally.lock", "wally.toml"]

[dependencies]
